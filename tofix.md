我覺得儲存格的邊框太搶戲了 浪費空間且影響視線 過於笨重
加一個快捷按鈕來新增igonb
儲存格的編輯器文字大小要受上面的設定控制
要可以選取任意儲存格並在其之後加入新儲存格 並且儲存格要可以拖動

為啥go儲存格不行
func abc() string {
return "abc"
}

多輪輸出會卡住
import "github.com/HazelnutParadise/insyra/isr"

for i := range 10 {
isr.DL.Of(1,2,3).Show()
}

abc()

當一個儲存格runing時 下面的應該是waiting
不要限制畫面總高度 應該可以捲動無限延伸
